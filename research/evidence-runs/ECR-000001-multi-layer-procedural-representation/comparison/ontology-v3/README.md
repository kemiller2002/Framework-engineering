# Procedural Ontology v3

This directory contains the dimensional ontology used by Comparator v3 and the v3 normalizer validator.

Purpose:
- separate procedural role from object
- reduce over-merging caused by noun overlap
- keep literal disagreement visible while providing a more precise semantic aid

This ontology is provisional and should be treated as a measurement tool, not a theory claim.
